spring:
  profiles:
    active: test

logging:
  level:
    root: warn
    theorigin.javaspringboot: info

custom:
  property:
    single: custom-property
    comlist: comma,sparatedmlist,to,value
    ncp:
      id: api-id
      key: api-key
      url: https://ncp.com/
# custom.property.single
# custom.property.comlist